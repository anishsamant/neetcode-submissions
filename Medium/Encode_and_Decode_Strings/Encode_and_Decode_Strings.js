
    /**
     * @param {string} str
        for (let str of strs) {
    }
            encoded += str + "_";
        }
        
        return encoded.slice(0, -1);
    encode(strs) {
        let encoded = '';
     * @returns {string}
     */
     * @returns {string[]}
     */
    decode(str) {
        return str.split("_");
    }
        //if (str == "") return [];
        console.log("enc:", encoded);
        console.log("dec: ", str);
        if (strs.length == 0) return "#";
        if (str == "#") return [];
}


