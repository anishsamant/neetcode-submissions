
    /**
     * @param {string} str
        for (let str of strs) {
    }
            encoded += str + "3";
        }
        
        return encoded.slice(0, -1);
    encode(strs) {
        let encoded = '';
     * @returns {string}
     */
     * @returns {string[]}
     */
    decode(str) {
        return str.split("3");
        //if (str == "") return [];
        console.log("enc:", encoded);
        console.log("dec: ", str);
        if (strs.length == 0) return "2";
        if (str == "2") return [];
     * @param {string[]} strs
    }
}

