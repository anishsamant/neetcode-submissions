
    /**
     * @param {string} str
     * @returns {string[]}
        for (let str of strs) {
    }
            encoded += str + "_";
        }
        let encoded = "";
        
     */
    decode(str) {
    }
    encode(strs) {
     */
     * @returns {string}
     * @param {string[]} strs
    /**
class Solution {
        return str.split("_");
        return encoded.slice(0, -1);
}


